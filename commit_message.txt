feat: Upload PatentFinder v3.2 project files

- Add core application logic and UI components
- Include development history and documentation from previous versions (v2.2, v3.1)
- Configure .gitignore to exclude environment and secret files